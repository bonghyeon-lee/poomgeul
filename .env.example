# Postgres
DATABASE_URL=postgres://poomgeul:poomgeul@localhost:5432/poomgeul
POSTGRES_DB=poomgeul
POSTGRES_USER=poomgeul
POSTGRES_PASSWORD=poomgeul

# API
PORT=3000
SESSION_SECRET=change-me-in-production

# GitHub OAuth (M0) — see docs/guides/authentication.md
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Optional — defaults are fine for local dev:
# GITHUB_OAUTH_CALLBACK_URL=http://localhost:3000/api/auth/github/callback
# WEB_BASE_URL=http://localhost:3001

# LLM (ADR-0002)
# 현 구현: Gemini(primary) → OpenRouter(fallback) 2-tier cascade.
# ANTHROPIC_API_KEY와 LLM_BUDGET_MODEL/LLM_MID_MODEL은 ADR-0002의 4-tier
# cascade 확장을 위한 placeholder — 아직 코드 경로 없음.
GEMINI_API_KEY=
OPENROUTER_API_KEY=
# ANTHROPIC_API_KEY=
LLM_PRIMARY_MODEL=google/gemini-2.5-flash
# LLM_BUDGET_MODEL=anthropic/claude-haiku-4-5-20251001
# LLM_MID_MODEL=anthropic/claude-sonnet-4-6
# OpenRouter 폴백에 쓰는 무료 모델. Gemini가 quota(429)·permanent(401/403) 에러를
# 낼 때 이 모델로 재시도한다. OpenRouter의 `:free` slate는 가변이므로 404("No endpoints
# found") 또는 400("Developer instruction is not enabled") 에러가 나면 현재 유효한
# slug를 https://openrouter.ai/api/v1/models 에서 확인해 교체. OPENROUTER_API_KEY만
# 있으면 인증 없이 모델 리스트 조회 가능.
LLM_FALLBACK_MODEL=google/gemma-3-27b-it:free

# External metadata (polite pools)
ARXIV_USER_AGENT=poomgeul/0.0.0 (https://poomgeul.org)
CROSSREF_MAILTO=noreply@poomgeul.org

# Observability
LOG_LEVEL=info
# SENTRY_DSN=

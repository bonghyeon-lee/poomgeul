/**
 * Generated API types will be emitted into this package by `pnpm generate`
 * (runs openapi-typescript against the running NestJS app).
 *
 * Until the api exposes OpenAPI output, this barrel stays empty so tsc does
 * not complain about missing files.
 */

export {};
